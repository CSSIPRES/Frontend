import { RecapChaDirective } from './recap-cha.directive';

describe('RecapChaDirective', () => {
  it('should create an instance', () => {
    const directive = new RecapChaDirective();
    expect(directive).toBeTruthy();
  });
});
